BIND_ADDR=0.0.0.0:3000

# 對上 docker-compose.yml。host port 可用 SF_PG_PORT / SF_VALKEY_PORT 覆寫;
# 預設 PG 走 5433,因為 5432 在開發機上常被別的專案佔著。

# 接 Postgres adapter 後啟用(現為記憶體 store):
# DATABASE_URL=postgres://storefront:storefront@localhost:5433/storefront
# M4 起(快取):
# VALKEY_URL=redis://localhost:6379
