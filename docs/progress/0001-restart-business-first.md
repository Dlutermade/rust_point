# 0001 — 重啟:商業先行,清空實作

> 日期:2026-07-24。進度重製起點。

## 狀態

- 產品定名 **紅利點數中心**(loyalty points;**非**購物金 / 儲值)。
- **保留**:商業視角 `docs/plan/01-point-center/business/`(9 檔)+ `docs/tech/`(選型 / 佈局 / 觀測)+ `docs/plan/backlog.md`。
- **清空**:`projects/`(先前的 Rust core / adapters / migrations 全數移除)、`docs/plan` 的技術視角與舊 `api.md` / `internals.md`、`docs/plan/02-ledger-movements`、舊 progress `0001`–`0008`。
- 現階段:**先規劃、不實作**。

## 為什麼

- 一整輪商業釐清後模型大改——**點數是費用非負債**、發點**兩種批次形態**(齊發 / 聚合)、結帳**等待期批次入帳**、與**訂單中心解耦**——舊實作與舊技術規格已不對齊,留著只會誤導。
- **實作前提未足**:回饋計算(該發幾點:基礎 / 會員等級倍率 / 規則)與折抵幣值的歸屬**未定案**;會員、訂單整合也未明朗。故先把帳本規格定好,不急著寫程式。

## 待拍板(阻擋技術視角展開)

- 回饋計算走 **(A) 點數中心之外** 還是 **(B) 內建**(定了才知道要不要先規劃會員)。
- 永久點是否保留為 opt-in;生日禮是否獨立於一般發點;餘額頁是否顯示「未生效」總額。
- 三個前提假設:只折抵不折現、無等級倍率、不跨商家。
- code / context 是否由 `point-center` 改名 `loyalty-point-center`。

## 下一步

- 商業視角定稿(通讀 + 上列待拍板)。
- 之後才重寫技術視角(沙漏:對外 API → pod 間溝通 → pod 組成 → 資料存取),再動工。
